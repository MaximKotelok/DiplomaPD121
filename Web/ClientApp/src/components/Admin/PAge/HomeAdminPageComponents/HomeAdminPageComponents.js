import React, { useState } from "react";
import styles from "./HomeAdminPageComponents.module.css";

export const HomeAdminPageComponents = () => {
  return (
    <div className={`${styles["row-parent"]}`}>
      <div className={`${styles["box-container"]} `}></div>
    </div>
  );
};
